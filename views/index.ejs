<!DOCTYPE html>
<html>
  <head>
  </head>
  <body>
    <h1><%= title %></h1>
    <% articles.forEach(function(article){ %>
      <h3><%= article.title %></h3>
      <p><%= article.section1 %></p>
    <% }); %>
    <a href="/newArticle">Add new article</a>
  </body>
</html>
