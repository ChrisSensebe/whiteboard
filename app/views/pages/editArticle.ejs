<!DOCTYPE html>
<html>
    <head>
        <% include ../partials/head.ejs %>
        <% include ../partials/scripts.ejs%>
    </head>
    
    <body>
        
        <div id="wrap">
            <header>
                <% include ../partials/header.ejs %>
            </header>
            <div class="container">
                <% include ../partials/flash.ejs %>
                <h3>Edit article</h3>
                <div class="row">
                    <div class="col-md-12">
                        <form id="addArticle" name="addArticle" method="post" action="/app/update">
                            <legend>Article title</legend>
                            <% include ../partials/articleId.ejs %>
                            <% include ../partials/csrfToken.ejs %>
                            <input id="title" type="text" placeholder="Title" name="title" value="<%= article.title %>" class="form-control">
                            <legend>Article</legend>
                            <textarea id="article" placeholder="Article" name="article" class="form-control"><%= article.articleBody %></textarea>
                            <button type="submit" class="btn btn-primary">Save and exit</button>
                        </form>
                        <form>
                            <a href="/app/edit" class="btn btn-warning">Exit without saving</a>
                        </form>
                        <form id="delArticle" name="delArticle" method="post" action="/app/deleteArticle">
                            <% include ../partials/articleId.ejs %>
                            <% include ../partials/csrfToken.ejs %>
                            <button type='submit' class="btn btn-danger">Delete article</button>
                        </form>
                    </div>
                </div>
            </div>
            
        </div>
        
        <footer id="footer">
            <% include ../partials/footer.ejs %>
        </footer>
        
    </body>
</html>