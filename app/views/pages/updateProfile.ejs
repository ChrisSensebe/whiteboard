<!DOCTYPE html>
<html>
    <head>
        <% include ../partials/head.ejs %>
        <% include ../partials/scripts.ejs%>
    </head>
    
    <body>
        
        <div id="wrap">
            <header>
                <% include ../partials/header.ejs %>
            </header>
            
            <div class="container" id="page-content">
                <% include ../partials/flash.ejs %>
                <div class="row">
                    <div class="col-md-12">
                        <form action="/app/updateProfile" method="POST">
                            <legend>Profile</legend>
                            <% include ../partials/csrfToken.ejs %>
                            <input type="text" name="username" placeholder="<%= currentUser.username %>" required class="form-control">
                            <input type="password" name="password" placeholder="update your password" required class="form-control">
                            <input type="text" name="email" placeholder="<%= currentUser.email %>" required class="form-control">
                            <button type="submit" class="btn btn-primary">Update</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
        
        <footer id="footer">
            <% include ../partials/footer.ejs %>
        </footer>
        
    </body>
</html>